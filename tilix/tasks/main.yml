---

- name: "Tilix PPA"
  apt_repository:
    repo: "{{ tilix_ppa }}"
    update_cache: yes
    state: present
  sudo: yes

- name: "Tilix install"
  apt:
    name: tilix
    state: latest
  sudo: yes
