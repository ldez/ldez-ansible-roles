---

- name: "Ubuntu Make PPA"
  sudo: yes
  apt_repository:
    repo: "{{ ubuntu_make_ppa }}"
    update_cache: yes
    state: present

- name: "Ubuntu Make install"
  sudo: yes
  apt:
    name: ubuntu-make
    state: present
